# voraus-ros-bridge

Enables `voraus.core` integration within the ROS framework.

## Format the code

Run `cargo fmt`

## Analyze the code

Run `cargo clippy`

## Test the crate

Run `cargo test`

## Build the crate

Run `cargo build`
